<div hx-target="this" hx-swap="outerHTML" style="display: flex; justify-content: space-between; align-items: center;">
    <p id="recette_{{ idRecette }}">{{ quantite }}</p>
    <div  style="margin-left: auto;">
        <button type="button" class="btn btn-primary" hx-get="{{ path('modifierQuantite', {'idRecette': idRecette, 'quantite': quantite}) }}">Modifier</button>
    </div>
</div>